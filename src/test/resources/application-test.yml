spring:
  datasource:
    url: 'jdbc:postgresql://localhost:5432/custody-manager'
    username: custody-manager
    password: custody-manager

server:
  shutdown: immediate

management.endpoint:
  health.cache.time-to-live: 0
  info.cache.time-to-live: 0

hmpps:
  auth:
    url: http://localhost:8090/auth
